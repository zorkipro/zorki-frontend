# Улучшения диалоговых окон для мобильных устройств

## Описание проблемы

При работе с кабинетом блогера на мобильных устройствах были обнаружены следующие проблемы:

1. **Редактирование цены в платформе (Instagram)**: Диалоговое окно не открывалось при нажатии на кнопку редактирования
2. **Раздел "Цены"**: Диалоговое окно смещалось влево и вниз, перекрывалось клавиатурой
3. **Клавиатура**: Открывалась не цифровая клавиатура для ввода цен

## Реализованные решения

### 1. DialogContent (`src/components/ui/dialog.tsx`)

#### Улучшенное позиционирование
- **Мобильные устройства**: Диалог позиционируется в верхней части экрана (`top-[10vh]`), оставляя место для клавиатуры
- **Desktop**: Диалог центрируется по экрану (`top-[50%]`)
- **Максимальная высота**: 
  - Mobile: `max-h-[70vh]` - 70% видимой области
  - Desktop: `max-h-[85vh]` - 85% видимой области

#### Обработка iOS
- Автоматическое изменение viewport для предотвращения зума при фокусе на input
- Установка `maximum-scale=1` временно при открытом диалоге
- Восстановление оригинальных настроек viewport при закрытии

#### Предотвращение скроллинга body
- Автоматическое блокирование скролла страницы при открытом диалоге
- Восстановление скролла при закрытии
- MutationObserver для отслеживания состояния диалога

### 2. EditableCard (`src/components/ui/EditableCard.tsx`)

#### Вынос Dialog за пределы Card
**До:**
```tsx
<Card className="relative">
  <CardContent>
    <Button onClick={...}>Edit</Button>
    <Dialog>...</Dialog> {/* Внутри Card! */}
  </CardContent>
</Card>
```

**После:**
```tsx
<>
  <Card className="relative">
    <CardContent>
      <Button onClick={...}>Edit</Button>
    </CardContent>
  </Card>
  
  {/* Dialog вынесен за пределы Card для правильного портирования */}
  <Dialog>...</Dialog>
</>
```

**Причина**: Dialog должен портироваться в `document.body` через `DialogPortal`, но когда он находится внутри Card с `position: relative`, это может вызывать проблемы с позиционированием на мобильных.

### 3. PricingSection (`src/components/profile/organisms/PricingSection.tsx`)

#### Вынос Dialog за пределы сложной структуры
- Все диалоги вынесены из вложенной структуры кнопок и списков
- Создан отдельный массив диалогов, рендерящихся на верхнем уровне компонента
- Каждый диалог имеет уникальный `key={dialog-${platform}}`

#### Улучшенная структура
**До:**
```tsx
<Card>
  <div>
    <Button onClick={...}>Edit</Button>
    <Dialog>...</Dialog> {/* Глубоко вложен! */}
  </div>
</Card>
```

**После:**
```tsx
<>
  <Card>
    <div>
      <Button onClick={...}>Edit</Button>
    </div>
  </Card>
  
  {/* Диалоги на верхнем уровне */}
  {platforms.map(platform => (
    <Dialog key={`dialog-${platform}`}>...</Dialog>
  ))}
</>
```

### 4. Input (`src/components/ui/input.tsx`)

#### Автоматические оптимизации для мобильных
Для `type="tel"` или `inputMode="numeric"` автоматически добавляются:
- `inputMode="numeric"` - цифровая клавиатура на всех устройствах
- `pattern="[0-9]*"` - дополнительная поддержка для iOS
- `autoComplete="off"` - отключение автозаполнения
- `autoCorrect="off"` - отключение автокоррекции
- `spellCheck={false}` - отключение проверки орфографии

#### Touch-оптимизации
- `touch-manipulation` - улучшенная обработка touch-событий
- `text-[16px]` - размер шрифта 16px для предотвращения зума на iOS (Safari автоматически зумит при размере < 16px)

## Технические детали

### Портирование Dialog
Radix UI Dialog использует `Portal` для рендеринга контента в `document.body`. Это гарантирует:
- Правильное позиционирование относительно viewport
- Корректная работа z-index
- Отсутствие конфликтов с overflow: hidden родительских элементов

### Viewport и клавиатура
На мобильных устройствах появление клавиатуры изменяет визуальную высоту viewport:
- `vh` единицы не обновляются при появлении клавиатуры на Android
- `dvh` (dynamic viewport height) лучше, но поддерживается не везде
- Используется фиксированное позиционирование `top-[10vh]` с `max-h-[70vh]`
- Это оставляет 20vh для клавиатуры и окружения

### iOS специфика
iOS Safari имеет особенности:
- Автоматический зум при фокусе на input < 16px
- Viewport изменяется при появлении клавиатуры
- Требуется `maximum-scale=1` для предотвращения зума

## Результаты

### ✅ Исправлено
1. ✅ Диалог открывается при редактировании цены в платформе (Instagram)
2. ✅ Диалог правильно позиционируется в разделе "Цены"
3. ✅ Диалог по центру свободной от клавиатуры области
4. ✅ Цифровая клавиатура открывается для всех полей ввода цен
5. ✅ Предотвращен зум на iOS при фокусе на input
6. ✅ Заблокирован скролл страницы при открытом диалоге

### 📱 Тестирование
Рекомендуется протестировать на:
- iOS Safari (iPhone)
- Android Chrome
- Android Firefox
- Различные размеры экранов (особенно маленькие телефоны)

### 🎯 UX улучшения
- Плавные анимации открытия/закрытия
- Touch-friendly кнопки (увеличенная область нажатия)
- Правильная клавиатура для ввода цифр
- Нет непредвиденных зумов
- Диалог всегда видим, не перекрыт клавиатурой

## Файлы изменены
- `src/components/ui/dialog.tsx` - улучшено позиционирование и добавлена обработка мобильных устройств
- `src/components/ui/EditableCard.tsx` - Dialog вынесен за пределы Card
- `src/components/profile/organisms/PricingSection.tsx` - Dialogs вынесены на верхний уровень
- `src/components/ui/input.tsx` - добавлены автоматические оптимизации для мобильных

## Совместимость
- ✅ Desktop: работает как прежде
- ✅ Mobile: значительные улучшения
- ✅ Без breaking changes
- ✅ Обратная совместимость сохранена
